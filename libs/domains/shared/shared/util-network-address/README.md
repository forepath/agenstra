# util-network-address

Shared helpers for **hostname allowlist parsing** and **private / loopback IP detection** (IPv4, IPv6, and IPv4-mapped IPv6). Used by backend agent-controller client-endpoint SSRF checks and frontend runtime `CONFIG` URL validation.
